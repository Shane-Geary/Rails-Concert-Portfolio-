<%= form_for(@venue) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.submit "Submit" %> 
<% end %>

<p><h4><%= link_to "Delete Venue", venue_path, method: "delete" %></h4></p>